<%= form_for [@category, @mapping] do |form| %>
  <% if @mapping.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mapping.errors.count, "error") %> prohibited this mapping from being saved:</h2>

      <ul>
        <% @mapping.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>

    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_filter_id %>
    <%= form.number_field :category_filter_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>